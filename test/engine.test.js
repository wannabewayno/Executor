// unit tests for the engine.js
const engine = require('../lib/engine.js');

describe('engine.next(): Unit test',() => {
    it('should call the next question',() => {
        const testValue = 1;
        const expected = 1;
        expect().toBe();
    });
});

describe('engine.create(): Unit test',() => {
    it('should create the requested data and store it int he database',() => {
        const testValue = 1;
        const expected = 1;
        expect().toBe();
    });
});

describe('engine.delete(): Unit test',() => {
    it('should delete the requested data from the database',() => {
        const testValue = 1;
        const expected = 1;
        expect().toBe();
    });
});

describe('engine.update(): Unit test',() => {
    it('should update data from the database',() => {
        const testValue = 1;
        const expected = 1;
        expect().toBe();
    });
});

describe('engine.query(): Unit test', () => {
    it('Should point to database.js to perform SQL queries',() =>{
        const testValue = 1;
        const expected = 1;
        expect().toBe();
    });
});

describe('engine.buildPrompt(): Unit test', () => {
    it('Builds prompts dynamically from initial conditions',() =>{
        const testValue = 1;
        const expected = 1;
        expect().toBe();
    });
});